# Overview

I want to build a very simple application in HTML that have a button 
When I click on the button, it should open another page that will use google model viewer to display a 3D model of a pizza.
It will use ARKit to display the 3D model in the user's camera.

You need to set contrast and other setting for object also.

Android, Apple, Web

If device is not supported, it should show a message to the user.

